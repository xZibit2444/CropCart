RewriteEngine On

# Serve clean URLs (e.g., /farms -> /farms.html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([A-Za-z0-9-]+)/?$ $1.html [L]

# Root to index.html
RewriteRule ^$ index.html [L]
